# 网站发布到互联网 - 详细部署指南

> 本文档面向非技术人员，用通俗易懂的语言说明如何将网站发布到互联网上，让全球客户都能访问。

---

## 目录

1. [部署方式概述](#一部署方式概述)
2. [方案一：Vercel 免费部署（推荐）](#二方案一vercel-免费部署推荐)
3. [方案二：云服务器部署](#三方案二云服务器部署)
4. [方案三：虚拟主机部署](#四方案三虚拟主机部署)
5. [部署后的维护](#五部署后的维护)
6. [常见问题](#六常见问题)

---

## 一、部署方式概述

### 1.1 什么是"部署到互联网"？

简单说，就是把您电脑上的网站，放到一台**24小时开机**的互联网服务器上，这样全世界的人输入网址就能访问。

### 1.2 三种部署方案对比

| 方案 | 费用 | 技术难度 | 适合人群 | 推荐度 |
|-----|------|---------|---------|--------|
| **Vercel 免费部署** | 免费 | 简单 | 初学者、测试阶段 | ⭐⭐⭐⭐⭐ |
| **云服务器部署** | 每月几十~几百元 | 中等 | 正式运营、需要稳定 | ⭐⭐⭐⭐ |
| **虚拟主机部署** | 每年几百元 | 较复杂 | 有技术基础 | ⭐⭐⭐ |

---

## 二、方案一：Vercel 免费部署（推荐）

### 2.1 什么是 Vercel？

Vercel 是一个国外的网站托管平台，**个人使用免费**，特别适合 React 网站。全球访问速度快，支持自动部署。

### 2.2 部署前准备

需要准备：
1. **GitHub 账号**（代码托管平台）
2. **Vercel 账号**（用 GitHub 账号直接登录）

### 2.3 详细步骤

#### 第一步：注册 GitHub 账号

1. 打开 https://github.com
2. 点击 "Sign up"
3. 按提示填写邮箱、密码、用户名
4. 验证邮箱

#### 第二步：安装 Git

1. 下载 Git：https://git-scm.com/download/win
2. 双击安装，全部按默认设置点击"Next"
3. 安装完成后，在命令行输入 `git --version`，显示版本号即成功

#### 第三步：将代码上传到 GitHub

1. 打开命令行（CMD），进入项目文件夹：
   ```bash
   cd F:\Tvscode\HTTP1
   ```

2. 初始化 Git：
   ```bash
   git init
   ```

3. 添加所有文件：
   ```bash
   git add .
   ```

4. 提交代码：
   ```bash
   git commit -m "first commit"
   ```

5. 在 GitHub 网站上创建新仓库（New Repository）
   - 仓库名：`luxehair-wigs`
   - 选择 "Public"（公开）
   - 点击 "Create repository"

6. 连接本地代码到 GitHub：
   ```bash
   git remote add origin https://github.com/您的用户名/luxehair-wigs.git
   git branch -M main
   git push -u origin main
   ```
   > 按提示输入 GitHub 用户名和密码

#### 第四步：注册 Vercel 并部署

1. 打开 https://vercel.com
2. 点击 "Sign Up"，选择 "Continue with GitHub"
3. 授权 Vercel 访问您的 GitHub

4. 点击 "Add New Project"
5. 选择您刚创建的 `luxehair-wigs` 仓库
6. 配置项目：
   - **Framework Preset**：选择 `Vite`
   - **Root Directory**：保持默认（`./`）
   - **Build Command**：`npm run build`
   - **Output Directory**：`dist`

7. 点击 "Deploy"
8. 等待 2-3 分钟，部署完成

#### 第五步：访问您的网站

部署成功后，Vercel 会给您一个网址：
```
https://luxehair-wigs.vercel.app
```

把这个网址发给客户，他们就能访问了！

### 2.4 后续更新代码

当您修改了网站内容，需要重新部署：

```bash
cd F:\Tvscode\HTTP1
git add .
git commit -m "更新内容"
git push
```

Vercel 会自动检测到代码更新并重新部署！

---

## 三、方案二：云服务器部署

### 3.1 什么是云服务器？

云服务器就是一台**租用的电脑**，放在机房24小时运行。您可以完全控制这台电脑，适合正式商业运营。

### 3.2 推荐服务商

| 服务商 | 价格（每月） | 特点 |
|-------|-------------|------|
| 阿里云 | 99元起 | 国内访问快，需备案 |
| 腾讯云 | 99元起 | 国内访问快，需备案 |
| AWS（亚马逊） | 免费套餐12个月 | 全球访问快，免备案 |
| Vultr | $5起 | 免备案，全球节点 |

### 3.3 部署步骤（以 Vultr 为例）

#### 第一步：购买服务器

1. 打开 https://www.vultr.com
2. 注册账号并充值（最低 $10）
3. 点击 "Deploy New Server"
4. 选择：
   - **Server Type**：Cloud Compute
   - **Location**：Los Angeles（洛杉矶，离中国近）
   - **Operating System**：Ubuntu 22.04
   - **Plan**：$5/月（1GB内存）
5. 点击 "Deploy Now"

#### 第二步：连接服务器

1. 等待服务器创建完成（约5分钟）
2. 记录服务器的 IP 地址和密码
3. 下载连接工具：PuTTY（Windows）
4. 用 PuTTY 连接服务器：
   - Host Name：服务器IP地址
   - Port：22
   - 点击 Open
5. 登录：
   - 用户名：`root`
   - 密码：（粘贴服务器密码）

#### 第三步：安装环境

在服务器命令行中依次执行：

```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# 安装 PM2（进程管理器）
npm install -g pm2

# 安装 Nginx（Web服务器）
apt install -y nginx
```

#### 第四步：上传网站代码

**方法A：用 Git 拉取**
```bash
cd /var/www
git clone https://github.com/您的用户名/luxehair-wigs.git
cd luxehair-wigs
npm install
```

**方法B：用 FTP 上传**
1. 下载 FileZilla 客户端
2. 连接服务器（IP、用户名 root、密码）
3. 将本地文件上传到 `/var/www/luxehair-wigs`
4. 在服务器上执行 `npm install`

#### 第五步：配置后端服务

```bash
cd /var/www/luxehair-wigs

# 创建数据目录
mkdir -p data

# 启动后端服务
pm2 start api/server.ts --name "luxehair-api" --interpreter tsx

# 保存 PM2 配置
pm2 save
pm2 startup
```

#### 第六步：配置 Nginx

编辑 Nginx 配置文件：

```bash
nano /etc/nginx/sites-available/luxehair
```

粘贴以下内容：

```nginx
server {
    listen 80;
    server_name 您的域名或IP;

    # 前端静态文件
    location / {
        root /var/www/luxehair-wigs/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # 上传文件访问
    location /uploads {
        alias /var/www/luxehair-wigs/public/uploads;
    }
}
```

启用配置：
```bash
ln -s /etc/nginx/sites-available/luxehair /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

#### 第七步：构建前端

```bash
cd /var/www/luxehair-wigs
npm run build
```

#### 第八步：配置域名（可选）

1. 在域名服务商（如阿里云、GoDaddy）购买域名
2. 添加 A 记录，指向服务器 IP
3. 等待 DNS 生效（通常10分钟~24小时）

#### 第九步：配置 HTTPS（推荐）

```bash
# 安装 Certbot
apt install -y certbot python3-certbot-nginx

# 申请免费 SSL 证书
certbot --nginx -d 您的域名

# 按提示操作，选择 redirect（重定向HTTP到HTTPS）
```

### 3.4 访问网站

现在可以通过以下地址访问：
- `http://服务器IP` 或 `http://您的域名`
- `https://您的域名`（如果配置了HTTPS）

---

## 四、方案三：虚拟主机部署

### 4.1 什么是虚拟主机？

虚拟主机就是一台服务器上分割出很多小空间，每个用户用一个空间。价格便宜，但功能受限。

### 4.2 限制说明

⚠️ **注意**：本网站使用 Node.js + SQLite 数据库，**传统虚拟主机不支持**。

如果您一定要使用虚拟主机，需要：
1. 选择支持 Node.js 的主机（如阿里云云虚拟主机 Node.js 版）
2. 或使用静态网站托管（只部署前端，后端用第三方服务）

### 4.3 推荐：阿里云静态网站托管

如果只部署前端展示页面（不含后台管理）：

1. 购买阿里云 OSS 对象存储
2. 开启静态网站托管
3. 将 `dist` 文件夹内容上传到 OSS
4. 绑定域名

费用：每月几元钱（按流量计费）

---

## 五、部署后的维护

### 5.1 日常维护清单

| 频率 | 操作 |
|-----|------|
| 每天 | 登录后台查看新询盘 |
| 每周 | 检查网站是否正常访问 |
| 每月 | 备份数据库、检查服务器状态 |
| 每季度 | 更新依赖包、检查安全漏洞 |

### 5.2 数据备份

**Vercel 部署**：
- 代码在 GitHub 上，天然备份
- 数据库需要定期导出（Vercel 无持久化存储）

**云服务器部署**：
```bash
# 备份数据库
cp /var/www/luxehair-wigs/data/database.sqlite /backup/database-$(date +%Y%m%d).sqlite

# 备份代码
cd /var/www/luxehair-wigs
git add .
git commit -m "backup"
git push
```

### 5.3 更新网站内容

**Vercel**：
```bash
git add .
git commit -m "更新内容"
git push
# Vercel 自动部署
```

**云服务器**：
```bash
cd /var/www/luxehair-wigs
git pull
npm install
npm run build
pm2 restart luxehair-api
```

---

## 六、常见问题

### Q1：Vercel 免费版有什么限制？

- 每月 100GB 带宽
- 构建时间每月 6000 分钟
- 函数执行时间 10 秒
- 对于展示型网站完全够用

### Q2：为什么国内访问 Vercel 慢？

Vercel 服务器在国外，国内访问可能较慢。解决方案：
1. 使用云服务器部署（阿里云/腾讯云）
2. 使用 CDN 加速
3. 购买 Vercel Pro（有全球 CDN）

### Q3：需要备案吗？

| 服务器位置 | 是否需要备案 |
|-----------|-------------|
| 中国大陆（阿里云/腾讯云） | 需要 |
| 中国香港/海外（Vercel/Vultr） | 不需要 |

### Q4：域名怎么买？

推荐平台：
- 国内：阿里云、腾讯云（.com 约 60元/年）
- 国外：GoDaddy、Namecheap（.com 约 $10/年）

### Q5：客户数据安全吗？

- SQLite 数据库文件在服务器上
- 建议定期备份
- 重要数据建议导出保存

### Q6：可以绑定自己的域名吗？

可以：
- **Vercel**：在 Settings > Domains 中添加域名
- **云服务器**：在域名管理中添加 A 记录指向服务器 IP

### Q7：部署后图片上传不了？

检查：
1. 上传目录权限：`chmod 755 /var/www/luxehair-wigs/public/uploads`
2. 磁盘空间是否充足
3. Nginx 配置中的 uploads 路径是否正确

---

## 快速决策指南

**如果您是初学者，想快速上线：**
→ 选择 **Vercel 免费部署**

**如果您要正式运营，面向国内客户：**
→ 选择 **阿里云/腾讯云服务器 + 备案**

**如果您要正式运营，面向海外客户：**
→ 选择 **Vultr/AWS 服务器**

**如果预算有限，只需要展示：**
→ 选择 **Vercel 免费部署**

---

> **建议**：先用 Vercel 免费部署测试，确认无误后再购买服务器正式运营。
